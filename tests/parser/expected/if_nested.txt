def main : i32
    if a:
        if b:
            c
        else:
            d
    else:
        e
--- tree
module
  function 'def'
    binding_name
      identifier 'main'
    function_return_type
      named_type
        identifier 'i32'
    block
      if 'if'
        identifier 'a'
        block
          if 'if'
            identifier 'b'
            block
              identifier 'c'
            else 'else'
              block
                identifier 'd'
        else 'else'
          block
            identifier 'e'
--- round trip: ok
