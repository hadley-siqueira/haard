// generated by hdc -- do not edit
#include <cstdint>
#include <cstdio>
#include <new>

struct h0_1_Bag;

struct h0_1_Bag {
    int32_t h0_2_n;
    virtual bool m_contains_b6(int32_t h0_4_value);
    virtual ~h0_1_Bag();
};

int32_t h0_5_main();

// record 0057: 'a ** b' over a signed whole number. C++ has no operator
// for it. A negative exponent is 1/(a ** -b) truncated toward zero, which
// is 0 for every base but 1 and -1
static int64_t __power_i(int64_t base, int64_t exponent) {
    if (exponent < 0) {
        if (base == 1) {
            return 1;
        }

        if (base == -1) {
            return exponent % 2 == 0 ? 1 : -1;
        }

        return 0;
    }

    int64_t answer = 1;

    for (int64_t i = 0; i < exponent; i++) {
        answer *= base;
    }

    return answer;
}

// record 0057: 'a // b' FLOORS, where C++'s '/' truncates toward zero.
// The two differ exactly when the signs differ and the division is not exact
static int64_t __floor_div_i(int64_t a, int64_t b) {
    int64_t answer = a / b;

    if (a % b != 0 && ((a < 0) != (b < 0))) {
        answer--;
    }

    return answer;
}


int32_t h0_5_main() {
    int32_t h0_6_a = (int32_t)__power_i(2, 5);
    int32_t h0_7_b = (int32_t)__floor_div_i(7, 2);
    int32_t h0_8_c = (int32_t)__floor_div_i((0 - 7), 2);
    int32_t h0_9_d = (int32_t)((uint32_t)((0 - 8)) >> 28);
    h0_1_Bag h0_10_bag;
    h0_10_bag.h0_2_n = 5;
    if (!h0_10_bag.m_contains_b6(5)) {
        return 1;
    }
    if (h0_10_bag.m_contains_b6(4)) {
        return 2;
    }
    return h0_6_a + h0_7_b + h0_8_c + h0_9_d;
}

bool h0_1_Bag::m_contains_b6(int32_t h0_4_value) {
    return h0_4_value == this->h0_2_n;
}

h0_1_Bag::~h0_1_Bag() {
}

int main() {
    return h0_5_main();
}
--- exit: 46
