#ifndef HAARD_AST_ENUM_H
#define HAARD_AST_ENUM_H

#include "ast/named_type_descriptor.h"

namespace haard {
    class Enum : public NamedTypeDescriptor {
    public:
        Enum();

    };
}

#endif
