import cpp2

def sum : int
    @a : int
    @b : int

    return a + b


def pointer_stuff : void
    a = 0
    p = &a
    p[0][i + 0] = 0
    p.foo(2, 3)
    p->foo(2, foo(2, 3))

def main : int
    a = 0 as int
    b = sum(2, 3)
    println('Hello, world!')
    c = '\n'
    c = 'a'
    a = +1

    while i > 0:
        i = i + 1

        while i > 0 and i < 10:
            i = i + 1
            i = i + 1

    return 0
