def main : void
    n = |a| {    
        println(a)

        if a > 0:
            print("a > 0")

            my_array.map(|e| {
                return e + 1
            })
    }

    n = |a : int, b : int| -> int {
        n = |a : int, b : int| {
            return a + b
        }

        foo(| | { })

        return a + b
    }
