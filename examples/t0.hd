import     std  .  io    as io
import std  .  file
import std
.
memory as mem

import std.file

def main : i32
    @argc : i32
    @argv : char**

    pass

def sum : i32
    @a : i32
    @b : i32
    @c : a::b<i32, b<f32>>

    a::b
    a
    a<i32, i32>
    a::b<i32, i32>

    while a:
        a
        a
